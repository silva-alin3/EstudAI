
// EstudAI v2.2 - full checklist runtime overrides
const V22_STOP=new Set('para pela pelo pelas pelos uma umas uns como com sem sobre entre mais menos muito muita muitos muitas ainda isso essa esse essas esses deste desta dessa desse onde quando quem qual quais seus suas nosso nossa nossos nossas brasil brasileira brasileiro desafio desafios caminhos medidas impactos perspectivas papel forma meios'.split(' '));
const v22Tokens=x=>String(x||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9\s]/g,' ').split(/\s+/).filter(w=>w.length>4&&!V22_STOP.has(w));
const v22ThemeMatch=text=>{const keys=[...new Set(v22Tokens(state.theme))],words=new Set(v22Tokens(text)),hits=keys.filter(k=>words.has(k));return{keys,hits,ratio:keys.length?hits.length/keys.length:0}};

home=function(){const chosen=state.selfReportedWeak||AREAS[0];const detected=AREAS.slice().sort((a,b)=>state.difficultyScores[a]-state.difficultyScores[b])[0];const ordered=[chosen,...AREAS.filter(a=>a!==chosen).sort((a,b)=>state.difficultyScores[a]-state.difficultyScores[b])];const daily=Math.min(100,pct(state.dailyXp,60));return `<section class="hero"><span class="eyebrow">${esc(state.goal)}</span><h1>Seu estudo, do seu jeito.</h1><p>O EstudAI respeita o foco que você escolheu e usa seu desempenho para sugerir reforços, sem abandonar as outras áreas.</p><div class="hero-actions"><button class="primary" data-action="personal-study">▶ Estudo personalizado</button><button class="secondary" data-tab="redacao">✍ Treinar redação</button></div><div class="goal"><div><b>Meta de hoje</b><strong>${daily}%</strong></div><div class="progress"><i style="width:${daily}%"></i></div><small>${state.dailyXp}/60 XP hoje</small></div></section><section><div class="title-row focus-title"><h2>Foco recomendado</h2><span>personalizado para você</span></div><div class="focus-note"><b>🎯 Foco escolhido: ${chosen}</b>${detected!==chosen?`<span>🔎 O desempenho também indica reforço em ${detected}.</span>`:`<span>Seu desempenho confirma essa prioridade.</span>`}</div><div class="focus-grid">${ordered.map((a,i)=>`<article class="focus-card tone-${i}"><div class="focus-icon">${['📖','🌎','🧪','📐'][AREAS.indexOf(a)]}</div><div><small>${i===0?'PRIORIDADE ESCOLHIDA':(a===detected?'PONTO FRACO DETECTADO':'REVISÃO')}</small><h3>${a}</h3><p>${state.difficultyScores[a]}% de domínio estimado</p></div><button data-action="area-study" data-area="${a}">Praticar →</button></article>`).join('')}</div></section>`};

scoreEssay=function(text){const wc=countWords(text),parts=text.split(/\n+/).map(x=>x.trim()).filter(Boolean),pars=parts.length,low=text.toLowerCase(),sentences=text.split(/[.!?]+/).map(x=>x.trim()).filter(Boolean),connectors=['além disso','portanto','entretanto','contudo','assim','dessa forma','nesse sentido','por conseguinte','todavia','desse modo'].filter(x=>low.includes(x)),agent=['governo','estado','escola','sociedade','ministério','prefeitura','família','empresas'].find(x=>low.includes(x)),action=['deve','promover','criar','implementar','ampliar','garantir','fiscalizar','investir'].find(x=>low.includes(x)),tm=v22ThemeMatch(text),tips=[],strengths=[],errors=[];if(wc<40)return{total:0,comp:[0,0,0,0,0],tips:['Desenvolva uma redação completa antes da correção.'],strengths:[],errors:['Texto insuficiente para avaliar as cinco competências.'],offTopic:false,themeCoverage:0};const offTopic=tm.keys.length>=2&&tm.hits.length===0;const weakTheme=!offTopic&&tm.keys.length>=3&&tm.ratio<.3;if(offTopic)return{total:0,comp:[0,0,0,0,0],tips:[`Reescreva o texto mantendo a discussão centrada em “${state.theme}”.`,'Use os conceitos centrais do tema na tese, nos argumentos e na intervenção.'],strengths:wc>=200?['Você produziu um texto com extensão suficiente para desenvolver uma argumentação.']:[],errors:[`Fuga ao tema: não encontrei relação suficiente entre o texto e “${state.theme}”.`],offTopic:true,themeCoverage:0};let c1=wc>=260?180:wc>=180?160:120,c2=weakTheme?80:tm.ratio>=.5?180:160,c3=pars>=4&&sentences.length>=8?180:pars>=3?160:120,c4=connectors.length>=4?180:connectors.length>=2?160:120,c5=agent&&action?180:agent||action?120:80;if(wc>=200)strengths.push(`Boa extensão: ${wc} palavras permitem desenvolver as ideias.`);if(pars>=4)strengths.push('A organização em parágrafos ajuda a separar introdução, desenvolvimento e conclusão.');if(connectors.length>=3)strengths.push(`Você usou conectivos como “${connectors.slice(0,3).join('”, “')}”, favorecendo a progressão.`);if(agent&&action)strengths.push(`A intervenção apresenta agente (${agent}) e ação (${action}).`);if(tm.hits.length)strengths.push(`O texto dialoga com conceitos centrais do tema: ${tm.hits.slice(0,3).join(', ')}.`);if(wc<250)tips.push('Amplie os argumentos com causas, consequências, exemplos e explicações concretas.');if(pars<4)tips.push('Organize o texto em introdução, dois desenvolvimentos e conclusão.');if(connectors.length<3)tips.push('Varie conectivos entre frases e parágrafos para melhorar a progressão.');if(!agent||!action)tips.push('Detalhe a intervenção com agente, ação, meio de execução, finalidade e detalhamento.');if(weakTheme)tips.push(`A relação com o tema está superficial. Retome explicitamente “${state.theme}” na tese e nos argumentos.`);if(sentences.some(x=>x.split(/\s+/).length>38))errors.push('Há períodos muito longos; divida algumas frases para melhorar clareza e pontuação.');if(pars<3)errors.push('Pouca divisão em parágrafos prejudica a organização argumentativa.');if(connectors.length<2)errors.push('Há pouca variedade de mecanismos coesivos entre as ideias.');const comp=[c1,c2,c3,c4,c5],total=comp.reduce((a,b)=>a+b,0);return{total,comp,tips,strengths:strengths.length?strengths:['Há uma estrutura dissertativo-argumentativa identificável.'],errors,offTopic:false,themeCoverage:Math.round(tm.ratio*100)}};

essayResult=function(r){const names=['Norma-padrão','Tema e repertório','Argumentação','Coesão','Intervenção'];return `<div class="essay-result">${r.offTopic?`<div class="theme-alert danger"><b>⚠️ Fuga ao tema identificada</b><p>A redação não responde ao tema escolhido. No ENEM, fuga total ao tema zera a prova.</p></div>`:`<div class="theme-alert ok"><b>🎯 Aderência ao tema</b><p>Compatibilidade estimada: ${r.themeCoverage}%.</p></div>`}<div class="essay-score"><span>Nota estimada</span><strong>${r.total}</strong><small>/1000</small></div>${names.map((n,i)=>`<details class="competence" ${i===1?'open':''}><summary><span>${n}</span><b>${r.comp[i]}/200</b></summary><div class="progress"><i style="width:${r.comp[i]/2}%"></i></div></details>`).join('')}<div class="feedback-grid"><section class="feedback-box success"><h3>✅ O que você fez bem</h3>${r.strengths.length?r.strengths.map(t=>`<p>• ${esc(t)}</p>`).join(''):'<p>• Continue praticando para consolidar seus pontos fortes.</p>'}</section><section class="feedback-box warn"><h3>🎯 O que melhorar</h3>${r.tips.map(t=>`<p>• ${esc(t)}</p>`).join('')}</section>${r.errors.length?`<section class="feedback-box error"><h3>🔎 Erros identificados</h3>${r.errors.map(t=>`<p>• ${esc(t)}</p>`).join('')}</section>`:''}</div><button class="caramelo-ask" data-action="ask-caramelo-essay">🐶 Conversar com o Prof. Caramelo sobre esta redação →</button></div>`};

reviewExplanation=function(q,answer){const topic=inferTopic(q),letter=String.fromCharCode(65+q.c),correct=questionText(q.a[q.c]),chosen=answer==null?'nenhuma alternativa':questionText(q.a[answer]);if(answer===q.c)return `Você acertou porque a alternativa ${letter} responde diretamente ao comando usando o conceito de ${topic}. Explique com suas palavras por que “${correct.slice(0,180)}” resolve o problema para consolidar a habilidade.`;return `Você marcou ${answer==null?'nenhuma alternativa':String.fromCharCode(65+answer)}, mas a correta é ${letter}. Compare sua escolha (“${chosen.slice(0,150)}”) com a correta (“${correct.slice(0,180)}”). A habilidade central é ${questionSkill(q)}. Na próxima tentativa, destaque no enunciado os dados e a ação pedida antes de avaliar as alternativas.`};

reviewHtml=function(items,answers){return `<div class="page-head"><div><span class="eyebrow">ANÁLISE</span><h1>Revisão do simulado</h1><p class="muted">Abra cada questão para ver a explicação completa.</p></div><button class="ghost" data-action="new-quiz">Voltar</button></div><div class="review-list">${items.map((q,i)=>{const ok=answers[i]===q.c,topic=inferTopic(q);return `<details class="review-card ${ok?'ok':'no'}"><summary><span>${ok?'✅':'❌'} Questão ${i+1}</span><b>${q.area} • ${topic}</b></summary><div class="review-body"><h3>${questionText(q.q)}</h3><div class="review-tags"><span>📚 ${topic}</span><span>🎯 ${questionSkill(q)}</span></div><p><b>Sua resposta:</b> ${answers[i]==null?'Não respondida':`${String.fromCharCode(65+answers[i])} — ${questionText(q.a[answers[i]])}`}</p><p><b>Correta:</b> ${String.fromCharCode(65+q.c)} — ${questionText(q.a[q.c])}</p><div class="review-explain"><b>${ok?'Por que você acertou':'Como corrigir o raciocínio'}</b><p>${esc(reviewExplanation(q,answers[i]))}</p></div></div></details>`}).join('')}</div>`};

quizView=function(){const q=quiz.items[quiz.i],ans=quiz.answers[quiz.i],checked=quiz.checked[quiz.i],images=(q.files||[]).map(f=>typeof f==='string'?f:(f.url||f.path||'')).filter(Boolean);return `<div class="quiz-top"><button class="close" data-action="exit-quiz">✕</button><div><span>${esc(quiz.title)}</span><b>Questão ${quiz.i+1} de ${quiz.items.length}</b></div><div class="qprogress"><i style="width:${(quiz.i+1)/quiz.items.length*100}%"></i></div></div><article class="question-card"><span class="eyebrow">${q.year?'ENEM '+q.year:(q.topic||q.area)}</span>${q.context?`<div class="question-context">${questionText(q.context).replace(/\n/g,'<br>')}</div>`:''}${images.map(src=>`<figure class="question-figure"><button type="button" class="image-zoom" data-v22-image="${esc(src)}"><img class="question-img" src="${esc(src)}" alt="Imagem da questão"><span>🔎 Toque para ampliar</span></button><figcaption>${esc((src.split('/').pop()||'imagem').slice(0,45))}</figcaption></figure>`).join('')}<h2 class="question-command">${questionText(q.q)}</h2><div class="answers">${q.a.map((x,i)=>`<button data-answer="${i}" class="${ans===i?'selected':''} ${checked&&i===q.c?'correct':''} ${checked&&ans===i&&i!==q.c?'wrong':''}" ${checked?'disabled':''}><b>${String.fromCharCode(65+i)}</b><span>${questionText(x)}</span></button>`).join('')}</div>${checked?`<div class="feedback ${ans===q.c?'good':'bad'}"><h3>${ans===q.c?'✅ Resposta correta!':'❌ Resposta incorreta'}</h3>${ans!==q.c?`<p><b>Resposta correta:</b> ${String.fromCharCode(65+q.c)} — ${questionText(q.a[q.c])}</p>`:''}<p>${esc(reviewExplanation(q,ans))}</p></div>`:''}</article><div class="quiz-bar">${!checked?`<button class="primary" data-action="confirm-answer" ${ans==null?'disabled':''}>Confirmar resposta</button>`:`<button class="primary" data-action="next-question">${quiz.i===quiz.items.length-1?'Ver resultado':'Próxima questão →'}</button>`}</div>`};

localProfAnswer=function(text){const users=state.chat.filter(m=>m.role==='user'),previous=users.length>1?users[users.length-2].text:'',combined=(previous+' '+text).toLowerCase(),weak=state.selfReportedWeak||AREAS.slice().sort((a,b)=>state.difficultyScores[a]-state.difficultyScores[b])[0];if(/como (usar|funciona)|usar o app|aplicativo/.test(combined))return 'No Início você recebe recomendações; em Simulados escolhe áreas e quantidade; em Desempenho acompanha sua evolução; e em Redação treina e recebe feedback. Durante um simulado, use o X para sair. 🐶📱✨';if(/matem|equação|porcent|geometr|funç/.test(combined))return `Em Matemática: 1) identifique o conteúdo; 2) destaque os dados; 3) escolha a estratégia; 4) resolva sem depender das alternativas; 5) revise o erro. ${weak==='Matemática'?'Como Matemática é seu foco escolhido, ela continuará sendo prioridade nos treinos personalizados.':'Vou manter seu foco em '+weak+' e incluir revisão frequente de Matemática.'} 📐🐶`;if(/redação|redacao|tese|argument|interven/.test(combined))return `Na redação sobre “${state.theme}”, confirme primeiro se a tese responde exatamente ao tema. Depois desenvolva dois argumentos diferentes e finalize com agente, ação, meio e finalidade. Se aparecer fuga ao tema, corrija a tese antes do restante. ✍️🎯🐶`;if(/desempenho|melhorar|dificuldade|ponto fraco|foco/.test(combined))return `Seu foco escolhido é ${weak}. Eu não vou trocar essa escolha silenciosamente. O painel pode detectar outra área com baixo desempenho, mas ela entra como reforço secundário. 📊🐾`;if(/hist[oó]ria|humanas|geografia|sociologia|filosofia/.test(combined))return 'Em Humanas, leia o comando primeiro, identifique contexto, causa e consequência e elimine alternativas que extrapolam a fonte. 🌎📚🐶';if(/natureza|biologia|qu[ií]mica|f[ií]sica/.test(combined))return 'Em Natureza, identifique o fenômeno e as grandezas envolvidas antes de aplicar fórmulas. Depois confira unidade e coerência do resultado. 🧪🔬🐶';if(/linguagem|portugu|interpreta|texto/.test(combined))return 'Em Linguagens, leia primeiro o comando e volte ao texto procurando a evidência que sustenta cada alternativa. Evite responder só pelo tema geral. 📖✨🐶';return `Posso te explicar passo a passo. Seu foco principal continua sendo ${weak}. Diga a matéria, a questão ou a parte do app que você quer entender e eu continuo daqui. 🐶📚✨`};

sendProf=async function(){const input=document.querySelector('#profInput'),text=input?.value.trim();if(!text)return;state.chat.push({role:'user',text});save();renderProfPanel();const typing=document.querySelector('#profTyping');if(typing)typing.innerHTML='<div class="msg assistant typing">Pensando na melhor forma de te explicar… 🐶💭✨</div>';const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),4500);try{const r=await fetch('https://estudai-app.vercel.app/api/prof-caramelo',{method:'POST',headers:{'Content-Type':'application/json'},signal:controller.signal,body:JSON.stringify({message:text,context:{selfReportedWeak:state.selfReportedWeak,difficultyScores:state.difficultyScores,history:state.history.slice(-5).map(h=>({area:h.area,correct:h.correct,total:h.total})),theme:state.theme,previousMessages:state.chat.slice(-5)}})});clearTimeout(timer);if(!r.ok)throw new Error('server');const d=await r.json();state.chat.push({role:'assistant',text:friendlyAnswer(d.answer||localProfAnswer(text))})}catch(e){clearTimeout(timer);state.chat.push({role:'assistant',text:localProfAnswer(text)})}save();renderProfPanel()};

initStatusBar=async function(){try{await StatusBar.setOverlaysWebView({overlay:false});await StatusBar.setBackgroundColor({color:'#0877B5'});await StatusBar.setStyle({style:Style.Light})}catch{document.documentElement.classList.add('status-fallback')}};
setTimeout(()=>initStatusBar(),0);

document.addEventListener('click',e=>{const z=e.target.closest?.('[data-v22-image]');if(z){e.preventDefault();const old=document.querySelector('.v22-image-modal');if(old)old.remove();const modal=document.createElement('div');modal.className='v22-image-modal';modal.innerHTML=`<div><button type="button" aria-label="Fechar">✕</button><img src="${esc(z.dataset.v22Image)}" alt="Imagem ampliada"></div>`;modal.onclick=ev=>{if(ev.target===modal||ev.target.closest('button'))modal.remove()};document.body.appendChild(modal)}});
